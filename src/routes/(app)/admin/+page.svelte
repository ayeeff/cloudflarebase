<script lang="ts">
  // The login form itself is rendered by the shared admin layout when the
  // visitor is not authenticated; this page is only ever reached while
  // unauthenticated, so there is nothing else to show.
</script>

<div class="p-6 text-sm text-muted-foreground">Sign in to continue.</div>
